# Copy this file to .env.local and fill in your values

# Paystack Integration
# Get your keys from https://dashboard.paystack.com/#/settings/developers
NEXT_PUBLIC_PAYSTACK_KEY=pk_test_xxxxxxxxxxxxx
PAYSTACK_SECRET_KEY=sk_test_xxxxxxxxxxxxx

# Download Security
# Generate a random string for signing download URLs
DOWNLOAD_SECRET=your-random-secret-key-here

# Newsletter + Transactional Email
# This project uses:
# - Brevo for subscriber management (UNLIMITED contacts on free plan)
# - Resend for transactional emails (3,000/month on free plan)

# Google Analytics
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://yourdomain.com
NEXT_PUBLIC_HARDCOPY_PRICE=99
NEXT_PUBLIC_EBOOK_PRICE=89

# Optional: S3 for book storage (recommended for production)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your-bucket-name

# Brevo (Subscriber management - UNLIMITED contacts on free plan!)
# Free plan: Unlimited contacts + 300 emails/day (9,000/month)
# 1) Sign up/login at https://www.brevo.com/
# 2) Go to Settings > API Keys and create a new API key
# 3) Create a List (e.g. "Newsletter") and copy its List ID
# 4) Add the List ID below (or multiple IDs separated by commas)
BREVO_API_KEY=xkeysib_xxxxxxxxxxxxx
BREVO_LIST_ID=1
# Or for multiple lists: BREVO_LIST_IDS=1,2,3

# Google Sheets (Optional - backup storage for subscribers)
# Subscribers are saved to BOTH Brevo and Google Sheets (if configured)
# If one fails, the other still works - they don't block each other
# See GOOGLE_SHEETS_SETUP.md for detailed setup instructions
# GOOGLE_SHEETS_ID=your_spreadsheet_id_here
# GOOGLE_SHEETS_NAME=Subscriptions
# GOOGLE_SHEETS_CREDENTIALS='{"type":"service_account",...}'

# Resend (Transactional emails - purchase confirmations, download links)
# Free plan: 3,000 emails/month (much better than MailerSend's 500/month)
# 1) Sign up/login at https://resend.com/
# 2) Go to API Keys and create a new key
# 3) Verify your sending domain: Resend > Domains > Add & Verify
# 4) Use an email from your verified domain below
# Note: If your domain is already registered on Resend, you can use it here too
RESEND_API_KEY=re_xxxxxxxxxxxxx
RESEND_FROM_EMAIL=your@verified-domain.com
RESEND_SENDER_NAME=Kofi Asiedu-Mahama

# Optional: webhook fallback sending (OFF by default to avoid duplicates)
# RESEND_WEBHOOK_SEND_EMAILS=true

# Admin Emails (purchase notifications)
ADMIN_EMAILS=etagbor@gmail.com,ennasiedu8@gmail.com

# Analytics Dashboard (optional - restrict who can view /analytics)
# If set, only /analytics?key=YOUR_SECRET will show the dashboard; others redirect to /
# ANALYTICS_SECRET=your-random-secret-here

# Supabase Database (for analytics tracking)
# Get these from: https://supabase.com → Your Project → Settings → API
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

